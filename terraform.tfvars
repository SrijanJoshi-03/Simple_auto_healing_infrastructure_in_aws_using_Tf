# All variables have safe defaults in variable.tf, so `terraform apply`
# works with zero overrides. Uncomment/override anything you want to
# change for your own AWS account, e.g.:
#
# ecr_account_id = "111122223333" # replace with the aws account id
